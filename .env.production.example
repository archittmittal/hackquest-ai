# ============================================================================
# SECURITY CONFIGURATION - Environment Variables
# ============================================================================
# IMPORTANT: This file contains sensitive configuration.
# NEVER commit this to version control!
# Use for production deployments only.

# ============================================================================
# APPLICATION SETTINGS
# ============================================================================
ENVIRONMENT=production
DEBUG=false
PORT=8000

# ============================================================================
# SECRET KEY & JWT
# ============================================================================
# Generate a secure secret key: python -c "import secrets; print(secrets.token_urlsafe(32))"
# NEVER use the default value in production!
SECRET_KEY=your-super-secret-key-minimum-32-characters-change-this-now

# JWT token expiration (in seconds)
JWT_EXPIRATION=86400  # 24 hours

# ============================================================================
# RATE LIMITING
# ============================================================================
# Rate limit format: requests/time_unit (e.g., "100/minute", "1000/hour")
RATE_LIMIT_DEFAULT=100/minute
RATE_LIMIT_STORAGE=memory://  # Use redis://localhost:6379 in production
RATE_LIMIT_STRATEGY=fixed-window

# ============================================================================
# CORS - CRITICAL FOR SECURITY
# ============================================================================
# ONLY include your actual production domains
# Format: comma-separated list of allowed origins
CORS_ORIGINS_PROD=https://app.example.com,https://www.example.com

# Allow credentials (cookies, auth headers) only if needed
CORS_ALLOW_CREDENTIALS=true

# ============================================================================
# DATABASE
# ============================================================================
MONGODB_URL=mongyour-mongodb-connection-string-here
DATABASE_NAME=hackquest

# ============================================================================
# EXTERNAL SERVICES
# ============================================================================
GROQ_API_KEY=your-groq-api-key-from-console
PINECONE_API_KEY=your-pinecone-api-key-from-dashboard

# ============================================================================
# SECURITY ENFORCEMENT
# ============================================================================
# Enforce HTTPS in production
ENFORCE_HTTPS=true

# Require authentication on protected routes
REQUIRE_AUTH=true
